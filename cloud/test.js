/*
const Employee = Parse.Object.extend("Employee");
const query = new Parse.Query(Employee);
query.equalTo("firstName", "rudson");

const results = query.find().then(e => console.log(e)); 
*/

// const Library = Parse.Object.extend("Library");
// const query = new Parse.Query(Library);
// query.find().then(e => console.log(e));
